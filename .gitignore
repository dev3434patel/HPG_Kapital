# Environment
.env

# Data files (contains user submissions and admin data)
# Note: These files are needed for deployment, so we keep them in git
# /data/*.json
# !/data/.gitkeep

# Storage
# Note: These files are needed for deployment, so we keep them in git
# /storage/*.json
# !/storage/.gitkeep

# Logs
*.log
/logs/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

